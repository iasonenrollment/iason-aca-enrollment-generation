spring:
  application:
    name: iason-aca-enrollment-generation
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: iason-config-server
    profiles:
      active: dev
